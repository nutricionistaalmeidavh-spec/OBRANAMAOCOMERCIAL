{
  "name": "obra-na-mao-comercial",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo comercial independente do Obra na Mão",
  "engines": {
    "node": ">=22.12 <23",
    "npm": ">=10"
  },
  "workspaces": [
    "apps/desktop",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test:web --if-present && npm run test:desktop --if-present",
    "build": "npm run build:web --if-present && npm run build:desktop --if-present",
    "test:web": "npm --prefix apps/web test",
    "build:web": "npm --prefix apps/web run build",
    "test:desktop": "npm --workspace apps/desktop test",
    "build:desktop": "npm --workspace apps/desktop run build",
    "typecheck:contracts": "cd apps/web && npm exec -- tsc --noEmit -p ../../tsconfig.json"
  }
}
